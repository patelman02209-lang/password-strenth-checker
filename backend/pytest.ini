[pytest]
testpaths = tests
pythonpath = .
addopts = --cov=app --cov-report=term-missing --cov-report=xml
filterwarnings =
    ignore::DeprecationWarning
markers =
    security: authentication, authorization, and abuse-resistant behavior (pytest security suite)
